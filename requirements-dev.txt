# Development / test dependencies.
# Install with:  pip install -r requirements-dev.txt
-r backend/requirements.txt

# Postgres driver: the data layer supports a Supabase Postgres backend, and its
# adapter/dialect modules are unit-tested here (with a fake connection — no live
# Postgres required), so the import must resolve.
psycopg[binary]>=3.2,<4

pytest>=8,<10
pytest-cov>=5
pytest-randomly>=3.15   # shuffles test order to catch isolation/state leaks
respx>=0.21
